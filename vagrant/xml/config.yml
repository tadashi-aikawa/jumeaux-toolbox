title: xml

one:
  name: One endpoint
  host: http://localhost/api/xml/one

other:
  name: Other endpoint
  host: http://localhost/api/xml/other


addons:
  log2reqs:
    name: plain
    config:
      encoding: utf8

